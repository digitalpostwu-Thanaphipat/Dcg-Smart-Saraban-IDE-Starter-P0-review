# AGENTS.md — Dcg Smart Saraban

## เริ่มงานทุกครั้ง

- อ่าน `docs/00-แผนหลักระยะที่-1.md` (แผนหลัก: ขอบเขต, 17 ชีต, ADR, Schema) และ `docs/10-แผนดำเนินงานระยะที่-1.md` (Gate, เฟส, Pilot, DoD) ก่อนออกแบบหรือแก้โค้ด
- อ่าน `CONTEXT.md`, `DESIGN.md`, `docs/adr/` ที่เกี่ยวข้อง และเอกสารใน `docs/` ตามงาน
- เมื่อเลขเฟสแผนหลักกับแผนดำเนินงานไม่ตรงกัน ใช้ `docs/phase-mapping.md`
- อ่าน `WORKFLOW.md` แล้วเลือกกระบวนการทำงานที่ตรงกับประเภทของงาน พร้อมสร้างหลักฐานตาม Completion Criteria ของกระบวนการนั้น
- สรุปขอบเขต ข้อสมมติ และ Acceptance Criteria ก่อนลงมือ
- หากข้อกำหนดขาดหรือขัดกัน ให้หยุดและบันทึกไว้ใน `docs/99-ประเด็นรอการตัดสินใจ.md`; ห้ามเดา
- เมื่อเอกสารขัดกัน: **`docs/00` ชนะ** เหนือแผนดำเนินงานและเอกสารย่อยที่ยังไม่ sync

## กติกาธุรกิจที่ห้ามละเมิด

- UI, เมนู, สถานะ, Error, Notification, Help และข้อความตอบจาก AI ต้องเป็นภาษาไทย 100%
- AI เป็นผู้ช่วยเสนอเท่านั้น ห้ามอนุมัติ ออกเลข ลงนาม หรือแก้เนื้อหาโดยไม่มีผู้ใช้ยืนยัน
- ต้องแยกผู้กลั่นกรองชั้นที่ 1 และชั้นที่ 2 ของเอกสารเดียวกัน
- เฉพาะผู้มีสิทธิ์ชั้นที่ 2 เท่านั้นที่ออกเลขหนังสือได้
- เอกสารที่ออกเลขแล้วห้ามแก้ทับ ใช้การยกเลิกหรือฉบับแทนที่พร้อม Audit Log เท่านั้น
- PDF ควบคุมเป็นผลลัพธ์สุดท้ายของระยะที่ 1; การลงนามทำภายนอกและอัปโหลด PDF ลงนามกลับเป็นหลักฐาน

## ข้อมูลและ Google Sheets

- ห้ามอ่านหรือเขียนข้อมูลโดยอ้างอิงลำดับคอลัมน์, A/B/C หรือ `row[index]`; ใช้ชื่อหัวคอลัมน์ภาษาไทยตาม Data Dictionary เท่านั้น
- ตรวจ Schema, หัวคอลัมน์ซ้ำ/หาย, รุ่นโครงสร้าง และค่าตรวจสอบก่อนนำเข้าหรือเขียนข้อมูลทุกครั้ง
- หน้าเว็บห้ามเข้าถึง Google Sheets โดยตรง; ต้องผ่านชั้น Backend/Apps Script ที่ตรวจสิทธิ์และบันทึก Audit
- ห้าม Hardcode ค่าธุรกิจ รูปแบบเอกสาร ฟอนต์ สี ขนาด ระยะขอบ SLA รายงาน หรือรอบสำรอง; อ่านจากค่าตั้งค่าที่ผ่านอนุมัติ
- ห้ามเก็บ Secret, Token, API Key, ข้อมูล HR เกินจำเป็น หรือเอกสารจริงของผู้ใช้ใน Repository

## Skill ภายนอก

- Skill เฉพาะโครงการใน `.agents/skills/` และกติกาในไฟล์นี้มีลำดับความสำคัญสูงกว่า Skill ภายนอกเสมอ
- ก่อนเพิ่มหรือใช้ Skill ภายนอก ต้องตรวจเนื้อหา `SKILL.md`, แหล่งที่มา, สิทธิ์ที่ต้องใช้, ผลกระทบ และบันทึกใน `skills.lock.md`
- ห้ามติดตั้งคลัง Skill ทั้งหมดแบบ Global หรือแบบ `--all`; ติดตั้งเฉพาะ Skill ที่จำเป็นในโฟลเดอร์โครงการและล็อกรุ่นหรือ commit
- Skill ภายนอกห้ามแก้ `AGENTS.md`, `CONTEXT.md`, `DESIGN.md`, สิทธิ์, Workflow, กฎสารบรรณ, Test หรือการตั้งค่าปล่อยระบบเอง
- หาก Skill ภายนอกขัดกับกติกาโครงการ ให้หยุดใช้งานและบันทึกประเด็นไว้ในเอกสารตัดสินใจ; ห้ามเลือกกติกาแทนเอง

## คุณภาพและการปล่อยระบบ

- ทุกการเปลี่ยน Workflow, สิทธิ์, เลขหนังสือ, PDF, Backup, Report หรือ Notification ต้องมี Unit Test และ Playwright E2E Test ที่เกี่ยวข้อง
- ฟีเจอร์ที่ผ่านการรับรองเป็น Protected Baseline; แก้ได้เมื่อมี Change Request ระบุเหตุผล ผลกระทบ Test และผู้อนุมัติ
- ห้ามแก้ Test เพื่อให้ผ่าน หาก Business Rule หรือ Acceptance Criteria ไม่ได้เปลี่ยน
- ห้าม Deploy Production อัตโนมัติ ต้องผ่าน CI, Staging, Code Review และ Human Approval
- รายงานผลทุกงานต้องระบุสิ่งที่แก้ ไฟล์ที่แก้ วิธีทดสอบ ผลจริง และความเสี่ยงที่เหลือ
- งานห้ามปิดจนกว่าจะมี Task Manifest และ Skill Evidence ตาม `WORKFLOW.md`; การอ้างชื่อ Skill โดยไม่มีหลักฐานผลลัพธ์ไม่นับว่าใช้ Skill
