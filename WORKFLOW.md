# WORKFLOW.md — กระบวนการควบคุมการทำงานของ Agent

เอกสารนี้แปลงแนวคิดการทำงานเชิงวิศวกรรมให้ใช้ได้แม้ IDE ไม่ได้ติดตั้ง Skill ภายนอก Agent ต้องเลือกกระบวนการที่ตรงกับงาน บันทึกใน Task Manifest และส่งหลักฐานตามเกณฑ์จบงาน ไม่ให้ถือว่าเพียงอ้างชื่อ Skill คือทำงานเสร็จ

## วิธีใช้ทุกงาน

1. สร้าง `docs/agent-runs/<รหัสงาน>-task.yml` จาก `templates/task-manifest.yml`
2. เลือกกระบวนการด้านล่างอย่างน้อย 1 รายการ และระบุ Skill เฉพาะโครงการ/Skill ภายนอกที่ได้รับอนุมัติจริง
3. ทำงานตามขั้นตอนและเกณฑ์จบงานของกระบวนการนั้น
4. สร้าง `docs/agent-runs/<รหัสงาน>-evidence.md` จาก `templates/skill-evidence.md`
5. ห้ามปิดงานจนกว่าผู้ตรวจรับจะตรวจหลักฐานและผลทดสอบได้

## กระบวนการที่ต้องเลือกใช้

| รหัส | กระบวนการ | ใช้เมื่อ | หลักฐานขั้นต่ำ |
|---|---|---|---|
| WF-01 | กลั่นกรองข้อกำหนด | เริ่มฟีเจอร์ เปลี่ยน Workflow/สิทธิ์/ข้อมูล หรือข้อกำหนดคลุมเครือ | คำถาม ข้อสรุป ข้อสมมติ และ ADR/รายการรอตัดสินใจ |
| WF-02 | ทำสเปกงาน | ก่อนสร้างหรือแก้พฤติกรรมระบบ | Spec, Acceptance Criteria, ขอบเขตไม่ทำ, ผลกระทบ |
| WF-03 | แตกงานและลำดับพึ่งพา | งานเกิน 1 ชิ้นหรือมีหลายชั้นระบบ | Ticket ย่อย เจ้าของงาน Dependency และเกณฑ์รับมอบ |
| WF-04 | พัฒนาแบบทดสอบนำ | แก้ Business Rule, API, Data Contract หรือ Bug | Test ที่ล้มเหลวก่อนแก้ ผลผ่านหลังแก้ และ Regression Test |
| WF-05 | ตรวจแบบสองแกน | ก่อนรวมโค้ดหรือปล่อย Staging | ผลตรวจตาม Spec และผลตรวจตามมาตรฐาน/ความปลอดภัย |
| WF-06 | สืบหาสาเหตุอย่างเป็นระบบ | Bug, ช้า, เลขซ้ำ, ข้อมูลผิด หรือระบบล่ม | ขั้นตอนทำซ้ำ หลักฐาน สมมติฐาน Root Cause และ Regression Test |
| WF-07 | ส่งต่องาน | เปลี่ยนผู้รับผิดชอบหรือจบ Session ที่ยังไม่เสร็จ | สถานะ ปัญหาค้าง ไฟล์/คำสั่งทดสอบ และงานถัดไป |
| WF-08 | ทบทวนสถาปัตยกรรม | ก่อนเพิ่มโมดูลใหญ่ หรือเมื่อโค้ดเริ่มซับซ้อน | แผนผังขอบเขตโมดูล Interface ผลกระทบ และ ADR |

## WF-01 กลั่นกรองข้อกำหนด

**เป้าหมาย:** ทำให้ Agent และเจ้าของงานใช้คำเดียวกันก่อนพัฒนา

1. อ่าน `CONTEXT.md`, `AGENTS.md`, `DESIGN.md`, `skills.lock.md` และเอกสารที่เกี่ยวข้อง
2. ระบุคำศัพท์/สถานะ/บทบาทที่กระทบ และตั้งคำถามเฉพาะจุดที่คำตอบเปลี่ยนพฤติกรรมระบบ
3. บันทึกข้อสรุปใน Spec; เรื่องที่ยังไม่มีคำตอบต้องลง `docs/99-ประเด็นรอการตัดสินใจ.md` หรือ ADR

**เกณฑ์จบงาน:** ไม่มีข้อสมมติที่มีผลต่อสิทธิ์ การออกเลข การล็อกเอกสาร ข้อมูลจริง หรือการปล่อยระบบโดยไม่มีเจ้าของตัดสินใจ

## WF-02 ทำสเปกงาน

**เป้าหมาย:** แปลงคำขอเป็นสัญญาที่ตรวจรับได้ก่อนเขียนโค้ด

ต้องระบุ: ปัญหา/เป้าหมาย, ผู้ใช้และสิทธิ์, Flow, Data Contract, UI ภาษาไทย, Acceptance Criteria, กรณีผิดพลาด, Test, ขอบเขตไม่ทำ และผลกระทบต่อ Backup/Report/Audit เมื่อเกี่ยวข้อง

**เกณฑ์จบงาน:** ผู้ตรวจสามารถตอบได้ว่าอะไรเปลี่ยน อะไรไม่เปลี่ยน และ Test ใดพิสูจน์ความสำเร็จ

## WF-03 แตกงานและลำดับพึ่งพา

**เป้าหมาย:** ไม่ให้ Agent กระโดดสร้างหลายส่วนจนตรวจยาก

แยกงานเป็นชิ้นเล็กที่ส่งมอบได้ เช่น Contract → Backend rule → UI → Test → Review โดยระบุงานที่ต้องเสร็จก่อนและงานที่ทำคู่ขนานได้

**เกณฑ์จบงาน:** ทุก Ticket มี Acceptance Criteria และไม่มีงานใดเริ่มข้าม Dependency สำคัญ

## WF-04 พัฒนาแบบทดสอบนำ

**เป้าหมาย:** ป้องกันการแก้โค้ดให้ดูผ่านโดยไม่รักษากฎธุรกิจ

1. เขียนหรือระบุ Test ที่พิสูจน์กฎก่อนแก้ และยืนยันว่า Test ล้มเหลวด้วยเหตุผลที่ถูกต้อง
2. แก้โค้ดทีละ Vertical Slice ให้ Test ผ่าน
3. Refactor โดย Test ต้องยังผ่าน และเพิ่ม E2E เมื่อกระทบผู้ใช้หรือ Workflow

**เกณฑ์จบงาน:** มี Unit Test และ/หรือ Playwright E2E ตามความเสี่ยง; ห้ามแก้ Test เพื่อให้ผ่านหาก Spec ไม่เปลี่ยน

## WF-05 ตรวจแบบสองแกน

**เป้าหมาย:** ตรวจทั้งความตรงตามคำขอและความถูกต้องตามมาตรฐาน

- แกน Spec: ตรง Acceptance Criteria, Flow, Role และ UI ภาษาไทยหรือไม่
- แกน Standards: ตรง `AGENTS.md`, Data Contract, Audit, Security, Design, Test และ Protected Baseline หรือไม่

**เกณฑ์จบงาน:** ข้อพบทั้งหมดถูกแก้ ยอมรับเป็นความเสี่ยงโดยผู้มีอำนาจ หรือเปิด Ticket ติดตามแล้ว

## WF-06 สืบหาสาเหตุอย่างเป็นระบบ

**เป้าหมาย:** แก้สาเหตุ ไม่ใช่อาการ

ทำตามลำดับ: ทำซ้ำปัญหา → ลดให้เหลือกรณีเล็กสุด → ตั้งสมมติฐาน → เก็บ Log/Metric ที่จำเป็น → ยืนยัน Root Cause → แก้ → Regression Test

**เกณฑ์จบงาน:** มีหลักฐานว่าการแก้ครอบคลุมสาเหตุ และมี Test ป้องกันการกลับมาเกิดซ้ำ

## WF-07 ส่งต่องาน

**เป้าหมาย:** ให้ Agent หรือคนถัดไปทำต่อได้โดยไม่เดา

ต้องระบุ: สิ่งที่ทำเสร็จ, สิ่งที่ยังไม่เสร็จ, การตัดสินใจ, ไฟล์ที่แตะ, คำสั่งทดสอบ/ผลล่าสุด, ความเสี่ยง และขั้นตอนถัดไป

**เกณฑ์จบงาน:** ผู้รับช่วงทำตามเอกสารแล้วเริ่มงานต่อได้โดยไม่ต้องค้นประวัติสนทนา

## WF-08 ทบทวนสถาปัตยกรรม

**เป้าหมาย:** รักษาโมดูลให้ชัดเจนและเปลี่ยนแปลงได้

ระบุขอบเขตของโมดูล Interface ที่เล็กและทดสอบได้ การไหลของข้อมูล/สิทธิ์ และผลกระทบต่อ Apps Script, Google Sheets, Drive, AI, Report และ Audit

**เกณฑ์จบงาน:** ไม่มีการให้หน้าเว็บคุยกับ Sheet โดยตรง, กฎธุรกิจไม่กระจายซ้ำ, และมี ADR เมื่อเลือกทางที่แก้ย้อนยาก

## ความเชื่อมโยงกับ Skill ภายนอก

Workflow นี้รับแรงบันดาลใจจากแนวทาง `grill-with-docs`, `to-spec`, `to-tickets`, `tdd`, `code-review`, `diagnosing-bugs`, `handoff` และ `improve-codebase-architecture` ของ Matt Pocock แต่เป็นคำสั่งเฉพาะ Dcg Smart Saraban ไม่ได้หมายความว่าติดตั้งหรือเรียกใช้ Skill ต้นฉบับแล้ว

