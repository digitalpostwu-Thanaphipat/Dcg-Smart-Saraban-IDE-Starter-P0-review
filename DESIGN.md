# DESIGN.md — Dcg Smart Saraban

เอกสารนี้เป็นมาตรฐานออกแบบประสบการณ์ผู้ใช้และหน้าจอของระบบ ไม่ใช่ที่เก็บกฎธุรกิจละเอียด ให้ใช้ร่วมกับ `AGENTS.md`, `CONTEXT.md` และเอกสารใน `docs/`

## หลักการออกแบบ

1. **ภาษาไทย 100%** — ป้ายกำกับ ปุ่ม สถานะ ข้อผิดพลาด ความช่วยเหลือ และข้อความ AI เป็นภาษาไทย
2. **เห็นงานถัดไปทันที** — ผู้ใช้ไม่ต้องค้นหาเองว่าต้องทำอะไร; Dashboard และการแจ้งเตือนต้องชี้ไปยังงานถัดไป
3. **เอกสารต้องดูได้ก่อนส่ง** — หน้าร่างมี A4 Preview แบบเรียลไทม์ควบคู่กับฟอร์มกรอกข้อมูล
4. **ป้องกันความผิดพลาดก่อนเกิด** — แสดงกฎ/ข้อมูลที่ขาดก่อนส่ง ไม่ใช้ข้อความผิดพลาดคลุมเครือหลังบันทึก
5. **ตรวจสอบย้อนหลังได้** — หน้ารายละเอียดเอกสารแสดงสถานะ รุ่น ผู้กระทำ เวลา และเหตุผลการส่งกลับอย่างชัดเจน
6. **เดสก์ท็อปเป็นหลัก มือถือสำหรับติดตาม/ตรวจงาน** — การร่างเอกสารและรายงานละเอียดออกแบบสำหรับจอใหญ่
7. **เรียบ สุภาพ และเข้าถึงได้** — ใช้ลำดับชั้นชัด ตัวอักษรอ่านง่าย คีย์บอร์ดใช้งานได้ และไม่พึ่งสีเพียงอย่างเดียว

## โครงสร้างเมนู

### ผู้ใช้งานทั่วไป

`หน้าหลัก`, `สร้างเอกสาร`, `เอกสารของฉัน`, `งานส่งกลับแก้ไข`, `เอกสารออกเลขแล้ว`, `การแจ้งเตือน`, `ค้นหา`, `ช่วยเหลือ`

### ผู้กลั่นกรองชั้นที่ 1

เพิ่ม `คิวกลางชั้นที่ 1`, `งานของฉัน`, `ประวัติการกลั่นกรอง`

### ผู้กลั่นกรองชั้นที่ 2

เพิ่ม `คิวกลางชั้นที่ 2`, `ออกเลขหนังสือ`, `ทะเบียนเลขหนังสือ`

### ผู้ดูแลระบบ

เพิ่ม `ผู้ใช้และสิทธิ์`, `แม่แบบและรูปแบบ`, `ค่าตั้งค่า`, `รายงาน`, `Audit Log`, `สถานะระบบและ Backup`

## หน้าจอหลัก

| หน้าจอ | องค์ประกอบและพฤติกรรมที่ต้องมี |
|---|---|
| หน้าหลัก | สรุปงานรอทำ การแจ้งเตือน งานใกล้เกิน SLA และทางลัดสร้างเอกสาร |
| สร้าง/แก้ร่าง | ฟอร์มข้อมูลด้านซ้าย, A4 Preview ด้านขวา, ตรวจข้อมูลบังคับ, บันทึกร่าง, AI เสนอแนะที่ผู้ใช้ต้องกดยอมรับ |
| เอกสารของฉัน | ค้นหา กรองสถานะ แสดงสถานะล่าสุดและวันที่ดำเนินการล่าสุด |
| คิวกลาง | แสดงงานตาม SLA/ความสำคัญ, ผู้รับผิดชอบ, สถานะ, ปุ่มรับงาน/เปิดตรวจ โดยไม่เปิดเผยข้อมูลเกินสิทธิ์ |
| หน้ากลั่นกรอง | Preview, checklist ตามประเภทหนังสือ, ข้อสังเกตแบบระบุตำแหน่ง, ส่งกลับ/ส่งต่อ พร้อมหน้าต่างยืนยัน |
| ออกเลขหนังสือ | แสดงสรุปเอกสาร รุ่นเนื้อหา ผู้กลั่นกรองก่อนหน้า และหน้าต่างยืนยันที่แจ้งว่าออกเลขแล้วแก้ทับไม่ได้ |
| รายละเอียดเอกสาร | Timeline แบบอ่านง่าย: ผู้กระทำ เวลา สถานะ เหตุผลส่งกลับ รุ่นเอกสาร เลขหนังสือ และไฟล์ PDF |
| การแจ้งเตือน | กระดิ่ง จำนวนยังไม่อ่าน ตัวกรอง และลิงก์ไปยังงานโดยตรง |
| รายงาน | ตัวกรองช่วงเวลา ปีงบประมาณ หน่วยงาน/หน่วยงานย่อย ประเภท สถานะ แล้วแสดงจำนวนเอกสารและภาระงานแยกกัน |
| ผู้ดูแลระบบ | จัดการข้อมูลอ้างอิง ค่าตั้งค่า รุ่นแม่แบบ รายงาน Backup และ Audit โดยต้องมีสิทธิ์เฉพาะ |

## กติกา A4 Preview และเอกสาร

- แสดงตัวอย่างกระดาษ A4 ในอัตราส่วนถูกต้อง มีระยะขอบ ฟอนต์ ระยะบรรทัด และย่อหน้าตามค่าตั้งค่ารุ่นที่เลือก
- แสดงสถานะ `ฉบับร่าง` ชัดเจนก่อนออกเลข และ `PDF ฉบับควบคุม` หลังออกเลข
- การแก้ค่าตั้งค่ารูปแบบมีผลต่อเอกสารใหม่เท่านั้น เอกสารเดิมต้องคงรุ่นเดิม
- หากข้อมูลบังคับไม่ครบ ให้ชี้ตำแหน่งและคำอธิบายภาษาไทย ไม่ล้างข้อมูลที่ผู้ใช้กรอก

## สถานะหน้าจอที่ต้องออกแบบทุกฟีเจอร์

- กำลังโหลด
- ไม่มีข้อมูล
- ไม่มีสิทธิ์เข้าถึง
- ข้อมูลไม่ครบหรือไม่ผ่านกฎ
- การเชื่อมต่อ/บริการขัดข้อง
- ดำเนินการสำเร็จ
- ต้องยืนยันก่อนทำรายการที่ย้อนกลับไม่ได้

ทุกสถานะต้องมีข้อความภาษาไทย สาเหตุที่เข้าใจได้ และปุ่มดำเนินการต่อที่เหมาะสม

## Design System และค่าตั้งค่า

- ฟอนต์ สี ขนาดตัวอักษร ระยะห่าง และรูปแบบปุ่มอ่านจาก `การตั้งค่าหน้าจอ` ที่ผ่านอนุมัติ
- ห้าม Hardcode สีหรือฟอนต์ใน Component; ใช้ตัวแปร Design Token ที่ Backend ส่งให้ UI
- ต้องมีค่าเริ่มต้นปลอดภัยเพื่อแสดงผลเมื่อค่าตั้งค่าผิดพลาด แต่ห้ามใช้ค่าเริ่มต้นนั้นแทนค่าธุรกิจ
- Component ที่ใช้ซ้ำ: ปุ่ม, สถานะ, กล่องแจ้งเตือน, ตาราง, ตัวกรอง, หน้าต่างยืนยัน, Timeline, A4 Preview และ Empty State

## การเข้าถึงและ Responsive

- ใช้คีย์บอร์ดเข้าถึงทุกปุ่มและฟอร์มได้
- ป้ายกำกับฟอร์มชัดเจน สีไม่ใช่สัญญาณเดียวของสถานะ
- รองรับการขยายตัวอักษรโดยไม่ทำปุ่ม/ข้อความทับกัน
- มือถือเน้น Dashboard, แจ้งเตือน, เปิดอ่าน, รับ/ส่งกลับงาน; การร่าง A4 เต็มรูปแบบให้แนะนำใช้จอใหญ่

## Definition of Done สำหรับงาน UI

- มี Loading, Empty, Error, No-permission และ Success State
- ข้อความผู้ใช้เป็นภาษาไทย 100% และไม่มีข้อความ Error ดิบจาก Library/Service
- ตรวจ Responsive สำหรับ Desktop และ Mobile ตามขอบเขตหน้าจอ
- มี Playwright E2E สำหรับเส้นทางสำคัญ และตรวจการเข้าถึงพื้นฐาน
- ไม่ซ่อนกฎธุรกิจหรือสิทธิ์ไว้เฉพาะใน UI; Backend ต้องตรวจซ้ำทุกครั้ง
