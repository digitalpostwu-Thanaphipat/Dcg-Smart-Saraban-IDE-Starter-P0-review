# บริบทสมบูรณ์สำหรับ IDE — Dcg Smart Saraban

ใช้ไฟล์นี้เป็นบริบทเริ่มต้นของ Coding Agent ร่วมกับ `AGENTS.md` และโฟลเดอร์ `docs/` ในชุด IDE

> ก่อนเริ่มงาน ต้องอ่าน `skills.lock.md` ด้วย Skill เฉพาะโครงการมีลำดับสูงกว่า Skill ภายนอก และใช้ Skill ภายนอกได้เฉพาะรายการที่ระบุว่า `อนุมัติใช้` เท่านั้น ห้ามติดตั้งคลัง Skill ทั้งหมดหรือให้ Skill ภายนอกเขียนทับกติกาโครงการ

> ต้องใช้ `WORKFLOW.md` ควบคุมทุก Task: สร้าง Task Manifest ก่อนเริ่ม เลือก Workflow ตามประเภทงาน และสร้าง Skill Evidence เมื่อจบงาน เพื่อแสดงหลักฐานผลลัพธ์ที่ตรวจสอบได้

## คำสั่งเริ่มต้น

ก่อนเขียนหรือแก้โค้ด ให้สรุปความเข้าใจ ขอบเขต ข้อสมมติ และ Acceptance Criteria หากข้อกำหนดสำคัญขาดหรือขัดกัน ห้ามเดา ให้บันทึกเป็นประเด็นรอการตัดสินใจ

ก่อนออกแบบ UI ให้เปิด `DESIGN.md` เพื่อใช้โครงเมนู หน้าจอ สถานะ A4 Preview และเกณฑ์การเข้าถึงเป็นมาตรฐาน

## เป้าหมาย

พัฒนาเว็บแอปภาษาไทย 100% ชื่อ Dcg Smart Saraban เพื่อช่วยผู้ใช้ร่าง ตรวจ และส่งหนังสือราชการเข้าคิวกลาง ให้เจ้าหน้าที่ส่วนอำนวยการและสารบรรณกลั่นกรองก่อนออกเลขหนังสือ ระยะที่ 1 ไม่แทนระบบสารบรรณเดิมทั้งหมด และไม่ลงนามในระบบ

รองรับหนังสือราชการ 6 ชนิด: หนังสือภายนอก, หนังสือภายใน, หนังสือประทับตรา, หนังสือสั่งการ, หนังสือประชาสัมพันธ์ และหนังสือที่เจ้าหน้าที่ทำขึ้นหรือรับไว้เป็นหลักฐาน

## Workflow ที่ห้ามเปลี่ยนเอง

ร่าง → รอส่ง → รอกลั่นกรองชั้นที่ 1 → ส่งกลับแก้ไข หรือ รอกลั่นกรองชั้นที่ 2 → ออกเลขแล้ว → แนบฉบับลงนามแล้ว

- เจ้าหน้าที่กลั่นกรองกลาง 4–5 คนใช้คิวร่วมกัน
- คนเดียวกันห้ามกลั่นกรองชั้นที่ 1 และ 2 ของเอกสารเดียวกัน
- เฉพาะผู้กลั่นกรองชั้นที่ 2 ที่ได้รับสิทธิ์ออกเลขได้
- ออกเลขแล้วสร้าง PDF ฉบับควบคุมและล็อกเนื้อหา
- ผู้ใช้รับ PDF ไปลงนามภายนอก แล้วอัปโหลด PDF ลงนามกลับเป็นหลักฐาน
- ยกเลิกหรือสร้างฉบับแทนที่ได้ แต่ห้ามแก้ทับเอกสารออกเลขแล้ว

## ข้อห้ามสำหรับ AI และโค้ด

- AI ช่วยร่าง/ตรวจเท่านั้น ห้ามอนุมัติ ออกเลข ลงนาม หรือแก้ข้อความโดยไม่มีผู้ใช้ยืนยัน
- ห้าม Hardcode ค่าธุรกิจ ฟอนต์ สี ขนาด ระยะขอบ ย่อหน้า SLA รูปแบบเลขหนังสือ รายงาน และรอบสำรอง
- ห้ามหน้าเว็บคุยกับ Google Sheets โดยตรง ต้องผ่าน Backend/Apps Script ที่ตรวจสิทธิ์และ Audit
- ห้ามอ้างอิงข้อมูล Google Sheets ด้วยลำดับคอลัมน์ A/B/C หรือ `row[index]`; ใช้ชื่อหัวคอลัมน์ภาษาไทยตาม Data Dictionary
- ห้าม commit Secret, Token, API Key, Spreadsheet ID ที่อ่อนไหว ข้อมูล HR เกินจำเป็น หรือเอกสารจริงของผู้ใช้

## Google Sheets และฐานข้อมูล

ชีตปฏิบัติงานขั้นต่ำ: `การตั้งค่าระบบ`, `การควบคุมโครงสร้าง`, `ประเภทหนังสือและแม่แบบ`, `ทะเบียนเอกสาร`, `เนื้อหาเอกสาร`, `รายการกลั่นกรอง`, `ทะเบียนเลขหนังสือ`, `ไฟล์เอกสาร`, `การแจ้งเตือน`, `บันทึกเหตุการณ์`, `บันทึกการนำเข้าข้อมูล`

- ตรวจหัวคอลัมน์หาย ซ้ำ เปลี่ยนชื่อ รุ่นโครงสร้าง และค่าตรวจสอบก่อนอ่าน/เขียนทุกครั้ง
- อ่าน/เขียนข้อมูลเป็นชุดและบันทึก Audit Log สำหรับการเปลี่ยนสำคัญ
- ใช้ Lock และรหัสคำขอเพื่อกันการออกเลขซ้ำหรือคำสั่งซ้ำ
- บุคลากร หน่วยงาน ตำแหน่ง มาจากโครงการแยก `Dcg ฐานข้อมูลกลางบุคลากรและโครงสร้างองค์กร`
- สิทธิ์ระบบเป็นข้อมูลเฉพาะ Dcg Smart Saraban ไม่อยู่ในฐานกลางบุคลากร

## โครงสร้าง Apps Script

แยก `server/entry`, `server/auth`, `server/domain`, `server/services`, `server/data`, `server/config`, `server/jobs`, `server/shared`, `client/`, `tests/` และ `scripts/` ตามเอกสาร `docs/08-โครงสร้าง-Apps-Script-ผ่าน-IDE.md`

- Router ห้ามมี Business Rule หรือเรียก Sheet โดยตรง
- ชั้น `data` เท่านั้นที่เรียก Spreadsheet/Drive Service
- HTML, CSS และ JavaScript แยกไฟล์; UI ใช้ `google.script.run` แบบ asynchronous
- อ่าน/เขียนแบบเป็นชุด ใช้ Cache สำหรับข้อมูลอ่านบ่อย และ Lock สำหรับงานสำคัญ
- หากใช้ TypeScript ต้อง Bundle ก่อน `clasp push` เพราะ Apps Script V8 ไม่รองรับ ES Modules โดยตรง

## ค่าตั้งค่าและแม่แบบ

ค่าตั้งค่าอยู่ในชีตแยก: `การตั้งค่าหน้าจอ`, `การตั้งค่ารูปแบบเอกสาร`, `การตั้งค่า Workflow`, `การตั้งค่ารายงาน`, `การตั้งค่าการสำรองข้อมูล` ทุกค่ามีรุ่น ผู้เสนอ ผู้อนุมัติ วันเริ่มมีผล เหตุผล และ Audit Log

กฎความปลอดภัยที่ตรึงในโค้ด: RBAC ฝั่ง Backend, การแยกผู้กลั่นกรอง 2 ชั้น, การล็อกหลังออกเลข, Audit Log และข้อห้ามหน้าเว็บเข้าถึง Sheets ตรง

## แจ้งเตือนในหน้าเว็บ

ต้องมีศูนย์แจ้งเตือนภาษาไทย: กระดิ่ง จำนวนยังไม่อ่าน รายการล่าสุด หน้ารวม และลิงก์ไปยังงานโดยตรง

แจ้งงานใหม่ชั้นที่ 1/2, ส่งกลับแก้ไข, ใกล้/เกิน SLA, ออกเลข/PDF พร้อม, ยกเลิก/แทนที่, Backup ล้มเหลว และโควตาใกล้เต็ม อีเมลไม่ใช่ช่องทางบังคับ

## รายงาน

รายงานต้องแยก:

- จำนวนเอกสาร: นับรหัสเอกสารไม่ซ้ำ
- ภาระงานกลั่นกรอง: นับทุกเหตุการณ์รับตรวจ ส่งกลับ ส่งต่อ และอนุมัติ
- จำนวนออกเลข: นับเหตุการณ์ออกเลขสำเร็จ
- ยกเลิก/แทนที่: แสดงแยก

กรองตามวัน เดือน ปีงบประมาณ หน่วยงาน หน่วยงานย่อย ประเภทหนังสือ สถานะ ผู้ร่าง และผู้กลั่นกรอง ใช้ข้อมูลสรุปที่ระบบสร้าง ไม่สแกนข้อมูลดิบทั้งหมดทุกครั้ง

## Backup และการกู้คืน

- รายวัน: ข้อมูลเปลี่ยนแปลง Audit Log PDF/ไฟล์ใหม่
- รายสัปดาห์: สำเนาเต็ม Sheets ไฟล์เอกสาร ค่าตั้งค่า และโค้ดที่ deploy
- รายเดือน: Export Excel/CSV และสำเนาระยะยาว
- ต้องทดสอบกู้คืนอย่างน้อยเดือนละครั้งในพื้นที่ทดสอบ ห้ามกู้ทับข้อมูลจริง

## การทดสอบและ Protected Baseline

ต้องมี Unit Test, Playwright E2E, Load, Concurrency, Soak และ Restore Test

เกณฑ์ UAT เริ่มต้น: ทดสอบผู้ใช้พร้อมกัน 50/100/200 คน, ออกเลขพร้อมกัน 20 คำขอโดยไม่มีเลขซ้ำหรือข้อมูลหาย, P95 ค้นหาไม่เกิน 2 วินาที, บันทึก/ส่งไม่เกิน 3 วินาที, รายงานสรุปไม่เกิน 3 วินาที

ฟีเจอร์ที่ผ่าน Unit Test, E2E และรับรองแล้วเป็น Protected Baseline แก้ได้เมื่อมี Change Request ระบุเหตุผล ผลกระทบ Test และผู้อนุมัติเท่านั้น Production ต้องผ่าน CI, Staging, Code Review และ Human Approval

## เรื่องที่ห้ามเดา

1. Google Sheets เป็นฐานปฏิบัติงานหลักหรือเฉพาะต้นแบบ/ข้อมูลอ้างอิง
2. วิธีเข้าสู่ระบบระยะที่ 1: Google Workspace หรือ SSO
3. SLA กลั่นกรองชั้นที่ 1 และ 2
4. RPO/RTO และระยะเก็บ Backup
5. ขอบเขตเอกสารจำกัดการเข้าถึง/ลับ
6. การเชื่อมระบบบันทึกงานเดิมในระยะที่ 1 หรือระยะถัดไป
7. ขอบเขตข้อมูลจริงที่อนุญาตให้ AI ประมวลผล
